id: esp32_dac
name: ESP32 Internal DAC
target: esp32
generic: true
help: |
  Classic ESP32 built-in DAC (GPIO25/26) for dac~ output. No external codec;
  adc~ capture is not available on this backend.

profile:
  Audio:
    ESPD_AUDIO_BACKEND_DAC: y

flasher:
  title: ESP32 DAC
  note: GPIO25/26
  image: assets/boards/esp32_dac.jpg
